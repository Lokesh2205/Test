pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'The Building the code hellooos'
                sh 'git checkout main'
            }
        }
        
    }
    
}

