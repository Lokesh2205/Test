pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the code hello's'
                sh 'git checkout main'
            }
        }
        
    }
    
}

