pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'The Building the code hellos'
                sh 'git checkout main'
            }
        }
        
    }
    
}

