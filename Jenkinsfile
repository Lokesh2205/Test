pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'The Building the code helloos'
                sh 'git checkout main'
            }
        }
        
    }
    
}

