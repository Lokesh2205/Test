pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the code hello'
                sh 'git checkout main'
            }
        }
        
    }
    
}

