pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'The Building the code hello's'
                sh 'git checkout main'
            }
        }
        
    }
    
}

